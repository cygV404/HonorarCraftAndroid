<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Kein Auto-Backup und kein Geraete-zu-Geraet-Transfer.

        Die Datenbank enthaelt Steuernummer, IBAN, BIC, Kundenanschriften und
        saemtliche Umsaetze. Mit allowBackup="true" waeren diese Daten in das
        Google-Drive-Backup des Nutzers geflossen - im Widerspruch zur Zusage im
        Ueber-Dialog ("Es findet keine Uebertragung an externe Server statt").

        Folge: bei einem Geraetewechsel gehen die Daten verloren. Ein eigener
        Export/Import ist dafuer der vorgesehene Weg.
    -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HonorarCraftAndroid">
        <activity
            android:name="de.v404.honorarcraftandroid.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.Starting"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
